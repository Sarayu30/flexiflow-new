specVersion: "v1"
serviceId: "flexiflow"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flexiflow-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
